# Настройка домена и Nginx для Flask приложения

## Предварительные требования

- ✅ Проект загружен на сервер в `/home/root/dauricrm`
- ✅ Виртуальное окружение создано и зависимости установлены
- ✅ Домен зарегистрирован на reg.ru
- ✅ У вас есть IP адрес вашего сервера

---

## Шаг 1: Настройка DNS на reg.ru

### 1.1 Подключитесь к панели управления reg.ru

1. Зайдите на https://www.reg.ru
2. Войдите в личный кабинет
3. Перейдите в раздел "Домены" → выберите ваш домен

### 1.2 Настройте DNS записи

**Вариант A: Использовать DNS reg.ru (рекомендуется)**

1. В настройках домена найдите "DNS-серверы" или "Управление DNS"
2. Убедитесь, что используются DNS reg.ru (ns1.reg.ru, ns2.reg.ru)
3. Перейдите в "Управление DNS-зонами" или "DNS-записи"

**Добавьте/измените следующие записи:**

```
Тип: A
Имя: @ (или оставьте пустым)
Значение: IP_вашего_сервера (например, 123.45.67.89)
TTL: 3600

Тип: A
Имя: www
Значение: IP_вашего_сервера (тот же IP)
TTL: 3600
```

**Пример:**
- Если ваш домен: `example.com`
- IP сервера: `123.45.67.89`

То записи должны быть:
```
A    @    123.45.67.89    3600
A    www  123.45.67.89    3600
```

4. Сохраните изменения
5. Подождите 5-30 минут для распространения DNS

**Вариант B: Использовать собственные DNS серверы**

Если хотите использовать DNS вашего сервера, укажите в reg.ru:
- ns1.ваш_домен.com
- ns2.ваш_домен.com

Но для начала используйте вариант A - проще.

### 1.3 Проверка DNS

Проверьте, что DNS записи применились:

```bash
# На вашем компьютере или на сервере
nslookup ваш_домен.com
# или
dig ваш_домен.com

# Должен вернуться ваш IP адрес
```

---

## Шаг 2: Создание systemd сервиса для Gunicorn

### 2.1 Создайте файл сервиса

```bash
# На сервере
sudo nano /etc/systemd/system/dauricrm.service
```

### 2.2 Вставьте следующее содержимое:

```ini
[Unit]
Description=Gunicorn instance to serve dauricrm
After=network.target

[Service]
User=root
Group=www-data
WorkingDirectory=/home/root/dauricrm
Environment="PATH=/home/root/dauricrm/venv/bin"
ExecStart=/home/root/dauricrm/venv/bin/gunicorn --workers 3 --bind unix:/home/root/dauricrm/dauricrm.sock -m 007 wsgi:app

[Install]
WantedBy=multi-user.target
```

**Важно:** 
- Замените `/home/root/dauricrm` на ваш реальный путь, если отличается
- Если используете другого пользователя (не root), замените `User=root` на вашего пользователя

### 2.3 Сохраните файл

- Нажмите `Ctrl+O` (сохранить)
- Нажмите `Enter` (подтвердить имя файла)
- Нажмите `Ctrl+X` (выйти)

### 2.4 Запустите и включите сервис

```bash
# Перезагрузите systemd
sudo systemctl daemon-reload

# Запустите сервис
sudo systemctl start dauricrm

# Включите автозапуск при загрузке системы
sudo systemctl enable dauricrm

# Проверьте статус
sudo systemctl status dauricrm
```

**Если видите ошибки**, проверьте логи:

```bash
sudo journalctl -u dauricrm -n 50
```

**Если всё ок**, увидите зелёный статус "active (running)".

---

## Шаг 3: Настройка Nginx

### 3.1 Установите Nginx (если еще не установлен)

```bash
sudo apt update
sudo apt install -y nginx
```

### 3.2 Создайте конфигурацию для вашего домена

```bash
sudo nano /etc/nginx/sites-available/dauricrm
```

### 3.3 Вставьте следующую конфигурацию:

**Замените `yourdomain.com` на ваш реальный домен!**

```nginx
server {
    listen 80;
    server_name yourdomain.com www.yourdomain.com;

    client_max_body_size 50M;

    location / {
        include proxy_params;
        proxy_pass http://unix:/home/root/dauricrm/dauricrm.sock;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /static {
        alias /home/root/dauricrm/static;
    }
}
```

**Пример для домена example.com:**

```nginx
server {
    listen 80;
    server_name example.com www.example.com;

    client_max_body_size 50M;

    location / {
        include proxy_params;
        proxy_pass http://unix:/home/root/dauricrm/dauricrm.sock;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /static {
        alias /home/root/dauricrm/static;
    }
}
```

### 3.4 Сохраните файл

- `Ctrl+O`, `Enter`, `Ctrl+X`

### 3.5 Активируйте конфигурацию

```bash
# Создайте символическую ссылку
sudo ln -s /etc/nginx/sites-available/dauricrm /etc/nginx/sites-enabled/

# Удалите дефолтную конфигурацию (опционально)
sudo rm /etc/nginx/sites-enabled/default

# Проверьте конфигурацию на ошибки
sudo nginx -t
```

**Если видите "syntax is ok" и "test is successful" - всё хорошо!**

### 3.6 Перезапустите Nginx

```bash
sudo systemctl restart nginx

# Проверьте статус
sudo systemctl status nginx
```

---

## Шаг 4: Настройка файрвола

```bash
# Разрешите HTTP и HTTPS
sudo ufw allow 'Nginx Full'
# или
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp

# Проверьте статус
sudo ufw status
```

---

## Шаг 5: Проверка работы сайта

### 5.1 Проверьте, что всё работает

1. Откройте браузер
2. Перейдите на `http://yourdomain.com` (без https пока)
3. Должен открыться ваш сайт!

### 5.2 Если не работает, проверьте логи

```bash
# Логи Nginx
sudo tail -f /var/log/nginx/error.log

# Логи приложения
sudo journalctl -u dauricrm -f

# Проверьте, что socket файл создан
ls -la /home/root/dauricrm/dauricrm.sock
```

---

## Шаг 6: Установка SSL сертификата (HTTPS)

### 6.1 Установите Certbot

```bash
sudo apt install -y certbot python3-certbot-nginx
```

### 6.2 Получите SSL сертификат

**Замените `yourdomain.com` на ваш домен!**

```bash
sudo certbot --nginx -d yourdomain.com -d www.yourdomain.com
```

**Пример:**

```bash
sudo certbot --nginx -d example.com -d www.example.com
```

### 6.3 Следуйте инструкциям Certbot

Certbot спросит:
1. **Email** - введите ваш email (для уведомлений о продлении)
2. **Согласие с условиями** - введите `A` (Agree)
3. **Поделиться email** - введите `Y` или `N` (по желанию)

Certbot автоматически:
- Получит сертификат
- Настроит Nginx для HTTPS
- Настроит автоматическое перенаправление с HTTP на HTTPS

### 6.4 Проверка автопродления

```bash
# Проверьте, что автопродление настроено
sudo certbot renew --dry-run
```

Если видите "The dry run was successful" - всё ок!

### 6.5 Проверьте HTTPS

Откройте в браузере: `https://yourdomain.com`

Должен открыться сайт с зелёным замочком 🔒

---

## Шаг 7: Финальная проверка

### 7.1 Проверьте все сервисы

```bash
# Gunicorn
sudo systemctl status dauricrm

# Nginx
sudo systemctl status nginx

# Оба должны быть "active (running)"
```

### 7.2 Проверьте сайт

- ✅ HTTP работает: `http://yourdomain.com` → автоматически перенаправляет на HTTPS
- ✅ HTTPS работает: `https://yourdomain.com` → открывается сайт
- ✅ WWW работает: `https://www.yourdomain.com` → открывается сайт

---

## Полезные команды для управления

### Перезапуск приложения

```bash
sudo systemctl restart dauricrm
```

### Перезапуск Nginx

```bash
sudo systemctl restart nginx
```

### Просмотр логов приложения

```bash
sudo journalctl -u dauricrm -f
```

### Просмотр логов Nginx

```bash
sudo tail -f /var/log/nginx/error.log
sudo tail -f /var/log/nginx/access.log
```

### Обновление приложения

```bash
# Остановите приложение
sudo systemctl stop dauricrm

# Загрузите новые файлы (через SCP с вашего компьютера)
# Или обновите через git pull

# Перезапустите
sudo systemctl start dauricrm
```

---

## Решение проблем

### Проблема: 502 Bad Gateway

**Решение:**

1. Проверьте, что Gunicorn запущен:
   ```bash
   sudo systemctl status dauricrm
   ```

2. Проверьте права на socket файл:
   ```bash
   ls -la /home/root/dauricrm/dauricrm.sock
   sudo chmod 777 /home/root/dauricrm/dauricrm.sock
   ```

3. Проверьте логи:
   ```bash
   sudo journalctl -u dauricrm -n 50
   ```

### Проблема: DNS не работает

**Решение:**

1. Проверьте DNS записи:
   ```bash
   nslookup yourdomain.com
   dig yourdomain.com
   ```

2. Подождите до 30 минут после изменения DNS

3. Очистите DNS кэш на вашем компьютере:
   ```bash
   # Windows
   ipconfig /flushdns
   
   # Linux/Mac
   sudo systemd-resolve --flush-caches
   ```

### Проблема: Nginx не запускается

**Решение:**

1. Проверьте конфигурацию:
   ```bash
   sudo nginx -t
   ```

2. Проверьте, не занят ли порт 80:
   ```bash
   sudo netstat -tulpn | grep :80
   ```

3. Проверьте логи:
   ```bash
   sudo tail -f /var/log/nginx/error.log
   ```

### Проблема: Certbot не может получить сертификат

**Решение:**

1. Убедитесь, что DNS записи настроены правильно
2. Убедитесь, что порт 80 открыт в файрволе
3. Убедитесь, что домен указывает на ваш IP
4. Подождите до 30 минут после настройки DNS

---

## Готово! 🎉

Ваш сайт должен быть доступен по адресу:
- `https://yourdomain.com`
- `https://www.yourdomain.com`

Если что-то не работает - проверьте логи и следуйте инструкциям по решению проблем выше.
